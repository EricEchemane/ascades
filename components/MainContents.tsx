import { Stack } from '@mui/material';
import React from 'react';
import { IUser } from '../schema/user.schema';

export default function MainContents({ user }: { user: IUser; }) {
    const [page, setPage] = React.useState(0);

    return <>
        <Stack>
            asd
        </Stack>
    </>;
}